// client/components/FullReportModal.tsx
import React from 'react';
import { X, FileText, Printer, Share2 } from 'lucide-react';

interface Section {
  heading: string;
  content: string;
}

interface ReportData {
  title: string;
  date: string;
  sections: Section[];
}

interface Props {
  data: ReportData;
  onClose: () => void;
}

export default function FullReportModal({ data, onClose }: Props) {
  return (
    <div className="fixed inset-0 z-[60] flex justify-center bg-slate-950/90 backdrop-blur-md overflow-y-auto pt-10 pb-20 animate-in fade-in duration-300">
      
      {/* Paper Container */}
      <div className="relative w-full max-w-3xl bg-slate-900 border border-slate-800 shadow-2xl rounded-xl overflow-hidden min-h-[80vh] flex flex-col mx-4 md:mx-0 h-fit">
        
        {/* Sticky Header */}
        <div className="sticky top-0 z-10 bg-slate-900/95 backdrop-blur border-b border-slate-800 p-4 flex justify-between items-center">
          <div className="flex items-center gap-2 text-slate-400 text-sm">
            <FileText className="h-4 w-4" />
            <span>Sentinel Intelligence Report</span>
          </div>
          <div className="flex items-center gap-2">
            <button className="p-2 hover:bg-slate-800 rounded text-slate-400 hover:text-white transition-colors" title="Print">
              <Printer className="h-4 w-4" />
            </button>
            <button onClick={onClose} className="p-2 hover:bg-red-900/20 rounded text-slate-400 hover:text-red-400 transition-colors">
              <X className="h-5 w-5" />
            </button>
          </div>
        </div>

        {/* Content Body */}
        <div className="p-8 md:p-12 space-y-8">
          
          {/* Title Section */}
          <div className="text-center border-b border-slate-800 pb-8">
            <div className="text-cyan-500 font-mono text-xs tracking-[0.2em] uppercase mb-4">Classified Briefing</div>
            <h1 className="text-3xl md:text-4xl font-bold text-white mb-4 leading-tight font-serif tracking-wide">
              {data.title}
            </h1>
            <div className="text-slate-500 font-mono text-sm">
              DATE: {data.date} • SECURITY LEVEL: PREMIUM
            </div>
          </div>

          {/* Sections Loop */}
          <div className="space-y-10">
            {data.sections.map((section, idx) => (
              <section key={idx} className="animate-in slide-in-from-bottom-4" style={{ animationDelay: `${idx * 100}ms` }}>
                <h2 className="text-xl font-bold text-amber-500 mb-3 flex items-center gap-3">
                  {section.heading}
                </h2>
                <div className="text-slate-300 leading-relaxed text-lg whitespace-pre-wrap font-serif">
                  {section.content}
                </div>
              </section>
            ))}
          </div>

          {/* Footer */}
          <div className="border-t border-slate-800 pt-8 mt-12 text-center">
            <p className="text-slate-600 text-xs italic">
              End of Report. Destroy after reading. <br/>
              Generated by Sentinel AI Core. Not financial advice.
            </p>
          </div>
        </div>

      </div>
    </div>
  );
}